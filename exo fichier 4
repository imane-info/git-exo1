#include<stdio.h>
int main (){

char c;
int nbr_ligne=1;
int nbr_caractere=0;
int nbr_mots=0;
FILE*fichier=fopen("C:\\Users\\LENOVO\\Desktop\\text.txt","r");

if (fichier==NULL){

    printf("erreur");
}
while((c=fgetc(fichier))!=EOF){
    nbr_caractere++;
    if(c=='\n'){  kml.omari1@gmail.com
        nbr_ligne++;
    }
    if(c==' '){
        nbr_mots++;
    }
}
    printf("le nombre total de caractere est %d",nbr_caractere);
    printf("le nombre total de lignes est %d",nbr_ligne);
    printf("le nombre total de mots est %d",nbr_mots);
    fclose(fichier);


}
